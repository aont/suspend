name: Deploy GitHub Pages (public)

on:
  push:
    branches: ["main"]
    # Deploy only when changes occur in public/ (skip condition)
    paths:
      - "public/**"
      - "scripts/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  actions: write
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.final_deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install terser
        run: npm install --no-save terser

      - name: Configure GitHub Pages
        if: ${{ vars.GITHUB_PAGES_URL == '' }}
        id: configure_pages
        uses: actions/configure-pages@v5

      - name: Save GitHub Pages URL
        if: ${{ vars.GITHUB_PAGES_URL == '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_PAGES_URL: ${{ steps.configure_pages.outputs.base_url }}
        run: |
          gh api --method POST "repos/${GITHUB_REPOSITORY}/actions/variables" \
            -f name=GITHUB_PAGES_URL \
            -f value="$GITHUB_PAGES_URL"

      - name: Build bookmarklet and index.html
        run: node scripts/build-bookmarklet.mjs
        env:
          GITHUB_PAGES_URL: ${{ vars.GITHUB_PAGES_URL || steps.configure_pages.outputs.base_url }}

      # Upload public/ as built as the Pages site artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          name: github-pages-final
          path: public

      - name: Deploy to GitHub Pages
        id: final_deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages-final
